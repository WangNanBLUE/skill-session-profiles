{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "^(startup|resume)$",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${PLUGIN_ROOT}/dist/hook/session-start.js\"",
            "statusMessage": "Restoring global skill defaults"
          }
        ]
      }
    ]
  }
}
